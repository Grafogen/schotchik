import React from 'react';

type titleProps={
    style:{color:string}
    children:React.ReactNode
}
const TitleComp = (props:titleProps) => {
    return (
        <div>
            {}
        </div>
    );
};

export default TitleComp;